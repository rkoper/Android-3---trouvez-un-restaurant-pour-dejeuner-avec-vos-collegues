<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/itemRecyclerView"
    android:layout_width="match_parent"
    android:layout_height="90dp"
    android:background="@color/colorWhite"
    android:padding="5dp"
    android:visibility="visible">

    <ImageView
        android:id="@+id/sub_photo_profil"
        android:layout_width="66dp"
        android:layout_height="66dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="4dp"
        android:background="@color/colorWhite"
        android:scaleType="centerCrop"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/sub_name_profil"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:maxLength="30"
        android:maxLines="1"
        android:text="@string/profil_name"
        android:textAppearance="@style/TextAppearanceBlack"
        android:textSize="15sp"
        app:layout_constraintStart_toEndOf="@id/sub_photo_profil"
        app:layout_constraintTop_toTopOf="@id/sub_photo_profil"
        app:layout_constraintBottom_toBottomOf="@id/sub_photo_profil"
        app:layout_constraintEnd_toEndOf="parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/Red"
        android:orientation="vertical"
        android:paddingBottom="25dp"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="5dp"
        tools:layout_editor_absoluteX="5dp"
        tools:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>